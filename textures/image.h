#ifndef RENDERER_IMAGE_H
#define RENDERER_IMAGE_H

/* Terrible practice I know. It's just the only way I know to compile the image into the executable.
 * Also, I don't want to mess around with pointers and write unsafe code.
 * I would add a fix me, but I will never get around to fix it.
 * I probably could've fixed it in the time that I wrote this but whatever.
 * */
#include "granite.c"
#include "brick.c"

#endif //RENDERER_IMAGE_H
